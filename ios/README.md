# iOS Platform Folder

This top-level folder now contains the iOS adapter package:

- `ai_ios_agent/`

Roadmap and protocol details live in `docs/framework/PLATFORM_ROADMAP.md` and `docs/framework/README.md`.
